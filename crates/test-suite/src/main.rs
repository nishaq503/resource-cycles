//! Tests for the workspace. This is a separate crate to ensure that the tests are run in a clean environment.

/// A simple test to ensure that the workspace is set up correctly.
fn main() {
    println!("Hello, world!");
}
